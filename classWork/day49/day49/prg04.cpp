#include<iostream>
#include<string>
void check_substr(char* string, const char* substring)
{
	bool flag;
	for (int i = 0;string[i] != '\0';i++)
	{
		if (string[i] == substring[0])
		{
			for(int j=1,k=0)
		}
	}
}